# Data Dictionary

Key tabular files and column-level notes used in this project.

This dataset folder currently stores cache JSON products (`*_cf4_cache.json`, `cf4_distance_cache.json`) rather than columnar tabular files.
For field-level schema, see the originating CF4 upstream catalog documentation referenced in `TERMS_OF_USE.md`.
